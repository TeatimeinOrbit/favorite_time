<!--フォロー・フォロー解除-->
<% if current_user.following?(user) %>
  <%= link_to "フォロー解除", public_user_relationships_path(user.id), method: :delete %>
<% else %>
  <%= link_to "フォロー", public_user_relationships_path(user.id), method: :post %>
<% end %>